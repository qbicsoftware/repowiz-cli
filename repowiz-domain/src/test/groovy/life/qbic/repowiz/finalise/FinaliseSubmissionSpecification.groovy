package life.qbic.repowiz.submit

import spock.lang.Specification

class FinaliseSubmissionSpecification extends Specification{

   /** def "Notification for failed submission"(){
        when:
        finaliseSubmission.submitData()

        then:
        thrown(IOException)
    }

    def "An exception is thrown if the user does not approve the submission"(){
        given:
        def verify = false

        when:
        def result = finaliseSubmission.verifyCorrectnessOfSubmission(verify)

        then:
        result
    }
 */
    

}
